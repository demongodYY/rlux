{
  "name": "rx-flux",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "mocha test/**/*.spec.js --reporter dot --exit",
    "test-w": "mocha test/**/*.spec.js --reporter dot -w"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "rx": "^4.1.0",
    "rxjs": "^6.4.0"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^6.1.1"
  }
}
